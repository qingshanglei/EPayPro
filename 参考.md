# vmqPro（基于 vmqphp 二次开发）发布日志

本项目基于 [szvone/vmqphp](https://github.com/szvone/vmqphp)（V免签/vPay v1.12）进行二次开发，原项目已长期停更（最后更新 2020-01-30）。本项目名为 **vmqPro**，在保留核心支付功能的基础上，重点进行了全面安全加固和现代化改造。

> ⚠️ 版本号统一在 `.env` 的 `APP_VERSION` 字段管理，每次发布更新时修改此值即可。

***

## 版本说明

- **上游项目**：[szvone/vmqphp](https://github.com/szvone/vmqphp)（V免签/vPay）
- **基于版本**：v1.12（2020-01-30，已停更）
- **本地版本号**：从 0.01 起始，独立递增
- **版本号规则**：每次发布新的本地修改，版本号 +0.01（如 0.01 → 0.02）

***

## 📋 版本 \[0.03] - 2026-05-15

## 🔍 功能概述

本次更新对后台管理系统进行全面 UI 优化，包括侧边栏导航图标添加、专业配色方案实施、仪表盘面板配色调整，所有文字与背景色对比度均符合 WCAG 2.1 AA 标准。

## 🖥️ UI/UX 优化

### 侧边栏导航图标

- **问题**：侧边栏5个菜单项仅显示纯文字，缺乏视觉图标引导，用户难以快速识别功能模块
- **修复**：后端 `getMenu()` 接口新增 `icon` 字段，前端渲染时将 Layui 图标插入菜单文字左侧，图标风格统一且表意直观
- **图标映射**：系统设置→`layui-icon-set`、监控端设置→`layui-icon-camera`、二维码管理→`layui-icon-code-circle`、订单列表→`layui-icon-form`、Api说明→`layui-icon-read`
- **影响文件**：
  - `application/index/controller/Index.php`
  - `public/aaa.html`

### 菜单选中态高亮

- **问题**：点击侧边栏菜单项后无视觉反馈，无法识别当前所在页面
- **修复**：新增菜单项点击事件监听，点击时添加 `layui-this` 类实现选中态高亮（左侧蓝色边框 + 高亮背景 + 白色文字）
- **影响文件**：
  - `public/aaa.html`

### 配色方案优化

- **问题**：系统沿用 Layui 默认深灰色调（`#393D49`），色彩对比度不足，视觉层次感弱，专业感欠缺
- **修复**：定义专业配色方案并覆盖 Layui 默认样式，包括：
  - 顶部导航栏背景 `#2c3e50`，文字 `#e2e8f0`
  - 侧边栏背景 `#1e2a3a`，默认文字 `#94a3b8`，悬停文字 `#e2e8f0`，选中文字 `#ffffff`
  - 侧边栏选中态左侧 3px 主色调（`#1a73e8`）边框
  - 主内容区背景 `#f5f7fa`，文字 `#1f2937`
- **影响文件**：
  - `public/css/global.css`

### 仪表盘面板配色优化

- **问题**：仪表盘6个统计面板使用 Layui 内置颜色类，与整体配色方案不协调
- **修复**：替换为自定义配色（蓝`#1a73e8`、绿`#34a853`、红`#ea4335`、琥珀`#f59e0b`、紫`#8b5cf6`、青`#06b6d4`），面板卡片背景改为白色并添加微妙阴影
- **影响文件**：
  - `public/main.html`

## ♿ 无障碍合规

### WCAG 2.1 AA 对比度验证

- **验证结果**：所有6组文字与背景色组合均通过 WCAG 2.1 AA 标准
  - 侧边栏默认文字（`#94a3b8` on `#1e2a3a`）：5.66:1 ✅
  - 侧边栏选中文字（`#ffffff` on `#253347`）：12.78:1 ✅
  - 内容区文字（`#1f2937` on `#f5f7fa`）：13.68:1 ✅
  - 头部文字（`#ffffff` on `#2c3e50`）：10.98:1 ✅
  - 头部导航项文字（`#e2e8f0` on `#2c3e50`）：8.91:1 ✅
  - 侧边栏悬停文字（`#e2e8f0` on `#253347`）：10.37:1 ✅

## 🎯 验证要点

1. ✅ 侧边栏5个菜单项均显示对应的 Layui 图标，图标位于文字左侧
2. ✅ 点击菜单项后显示选中态高亮（左侧蓝色边框 + 白色文字）
3. ✅ 顶部导航栏背景色为 `#2c3e50`，标题和管理员文字清晰可读
4. ✅ 侧边栏背景色为 `#1e2a3a`，菜单项默认文字色为 `#94a3b8`
5. ✅ 菜单项悬停时背景变亮、文字变亮，视觉反馈清晰
6. ✅ 主内容区背景色为 `#f5f7fa`，文字色为 `#1f2937`
7. ✅ 仪表盘6个统计面板图标背景色与整体配色协调
8. ✅ 所有文字与背景色对比度符合 WCAG 2.1 AA 标准
9. ✅ 所有原有功能逻辑不受影响，菜单点击仍能正确加载对应页面

***

## 📋 版本 \[0.02] - 2026-05-14

## 🔍 功能概述

本次更新聚焦 UI/UX 优化和二维码管理功能重构，包括删除冗余弹窗、修复前端 Bug、重构二维码管理页面、新增备注和状态控制功能。

## 🖥️ UI/UX 优化

### 删除登录使用协议弹窗

- **问题**：登录成功后弹出使用协议确认框，增加不必要的操作步骤
- **修复**：登录成功后直接跳转后台，移除 `layer.confirm` 协议弹窗逻辑
- **影响文件**：
  - `public/index.html`

### 修复支付页面 Vue 模板闪烁

- **问题**：页面加载时短暂显示 `￥{{reallyPrice}}` 原始模板语法
- **原因**：Vue 实例挂载前模板内容可见
- **修复**：添加 `v-cloak` 指令和对应 CSS `[v-cloak] { display: none; }`
- **影响文件**：
  - `public/payPage/pay.html`

### 删除"有更新啦"弹窗

- **问题**：上游项目已停更6年，`checkUpdate()` 持续弹窗干扰使用
- **修复**：移除 `checkUpdate()` 方法和前端调用
- **影响文件**：
  - `application/admin/controller/Index.php`
  - `public/aaa.html`

## 📱 二维码管理重构

### 侧边栏菜单合并

- **问题**：微信二维码和支付宝二维码分属两个菜单组，操作不便
- **修复**：合并为单一"二维码管理"入口
- **影响文件**：
  - `application/index/controller/Index.php`

### 添加二维码改为模态框

- **问题**：添加二维码为独立页面，操作流程割裂
- **修复**：改为管理页面内模态框（选择类型 + 上传二维码 + 输入金额和备注 + 保存）
- **影响文件**：
  - `public/admin/qrcodelist.html`

### 二维码管理表格重构

- **问题**：原表格信息不够直观，缺少状态控制
- **修复**：表格改为5列（类型、二维码图片、金额、备注、操作），操作列新增启用/禁用按钮，新增 `toggleQrcodeStatus()` 接口
- **影响文件**：
  - `application/admin/controller/Index.php`
  - `public/admin/qrcodelist.html`

### 新增备注列

- **问题**：无法标记收款账号信息
- **修复**：`pay_qrcode` 表新增 `remark` 字段，添加时可输入备注，管理表格显示备注列
- **影响文件**：
  - `vmq.sql`
  - `public/admin/qrcodelist.html`

### 新增状态控制

- **问题**：无法临时禁用某个收款账号，单号收款过多存在被封风险
- **修复**：`pay_qrcode` 表新增 `status` 字段（1启用/0禁用），`createOrder()` 查询时排除 `status=0` 的二维码
- **影响文件**：
  - `vmq.sql`
  - `application/index/controller/Index.php`
  - `application/admin/controller/Index.php`

## 🐛 问题修复

### 修复创建订单"订单超出负荷"错误

- **问题**：创建订单时频繁报"订单超出负荷"错误
- **原因**：ThinkPHP 5.1 `insert()` 第三参数 `true` 表示返回最后插入ID，`tmp_price` 无自增ID导致返回值恒为 0
- **修复**：改用参数化 `INSERT IGNORE` SQL，正确返回影响行数
- **影响文件**：
  - `application/index/controller/Index.php`

### 修复测试支付签名错误

- **问题**：测试支付时签名验证失败
- **原因**：示例文件硬编码密钥与系统实际密钥不一致
- **修复**：示例文件从 `.env` 动态读取 `VPAY_KEY`，密钥来源统一
- **影响文件**：
  - `public/example/main.php`
  - `public/example/notify.php`
  - `public/example/return.php`

### 修复"保存二维码"按钮无反应

- **问题**：点击保存二维码按钮无任何响应
- **原因**：`checkRate()` 函数验证通过时不返回 `true`；模态框表格分页导致 `table.cache` 数据不完整
- **修复**：`checkRate()` 添加 `return true`；移除模态框表格分页
- **影响文件**：
  - `public/admin/qrcodelist.html`

### 修复添加二维码 TypeError

- **问题**：添加二维码时报 `TypeError: Cannot read properties of undefined`
- **原因**：`layui.form.val('addForm').addType` 无法读取动态创建的表单
- **修复**：改用 `$('input[name="addType"]:checked').val()` 直接读取 DOM
- **影响文件**：
  - `public/admin/qrcodelist.html`

## 🎯 验证要点

1. ✅ 登录页面无使用协议弹窗，登录成功直接跳转后台
2. ✅ 支付页面加载时无 `￥{{reallyPrice}}` 模板闪烁
3. ✅ 后台无"有更新啦"弹窗
4. ✅ 侧边栏"二维码管理"菜单正常，无重复的微信/支付宝菜单
5. ✅ 点击"添加带金额的二维码"弹出模态框，可选择类型、上传图片、输入金额和备注
6. ✅ 保存二维码成功后表格自动刷新，显示新添加的记录
7. ✅ 二维码表格显示5列：类型、图片、金额、备注、操作
8. ✅ 点击启用/禁用按钮可切换二维码状态，状态变更即时生效
9. ✅ 测试支付功能正常，签名验证通过
10. ✅ 旧版监控 App 心跳上报正常

***

## 📋 版本 \[0.01] - 2026-05-14

## 🔍 功能概述

项目更名为 vmqPro 并完成安全加固及漏洞修复。原 vmqphp v1.12 长期停更（2020-01-30），存在多项高危漏洞（密码明文存储、SQL注入）等中危漏洞，经审计未发现恶意后门代码。

本次为 vmqPro 首次全面优化迭代，涵盖后端代码重构、安全加固、性能优化、数据库升级和前端改进。在保留全部核心支付功能的前提下，消除了大量技术债务和安全风险。

## 🔒 安全加固

### 管理员密码明文存储

- **问题**：管理员密码以明文存储在数据库中，存在严重安全隐患
- **修复**：实现 `PasswordService`，使用 bcrypt 哈希存储，兼容旧明文自动迁移
- **影响文件**：
  - `application/service/PasswordService.php`
  - `application/index/controller/Index.php`

### SQL 注入漏洞

- **问题**：`tmp_price` 表使用原生 SQL 拼接，存在 SQL 注入风险
- **修复**：改用参数化 `INSERT IGNORE` SQL
- **影响文件**：
  - `application/index/controller/Index.php`

### 调试模式未关闭

- **问题**：生产环境调试模式开启，可能泄露敏感信息
- **修复**：`app_debug=false`、`database.debug=false`
- **影响文件**：
  - `config/app.php`
  - `config/database.php`

### 敏感配置硬编码

- **问题**：数据库密码、通讯密钥等敏感信息硬编码在配置文件中
- **修复**：迁移至 `.env` 文件，从 `getenv()` 读取
- **影响文件**：
  - `config/database.php`
  - `.env.example`

## ⚡ 性能优化

### 数据库查询优化

- **问题**：`createOrder()` 中每次请求查询设置项 6-8 次，`getSettings()` 查询 11 次
- **修复**：实现 `SettingService` 请求级缓存，createOrder DB 查询 6-8次 → 1次，getSettings 11次 → 1次；`saveSetting` 使用事务；`getMain` 6次 → 2次
- **影响文件**：
  - `application/service/SettingService.php`
  - `application/index/controller/Index.php`
  - `application/admin/controller/Index.php`

### 订单清理效率

- **问题**：`closeEndOrder()` 逐条删除过期订单，效率低下
- **修复**：使用子查询批量删除 + 60秒间隔控制
- **影响文件**：
  - `application/index/controller/Index.php`

### 订单号生成

- **问题**：`rand(1,9)*4` 随机性不足，`Hms` 格式不含秒
- **修复**：改用 `mt_rand(1000, 9999)`，时间格式 `Hms` → `His`
- **影响文件**：
  - `application/index/controller/Index.php`

## 🏗️ 代码结构重构

### 公共服务层提取

- **问题**：HTTP 请求、签名验证、回调通知等逻辑分散在控制器中，大量重复代码
- **修复**：提取公共服务层 `HttpService`、`NotifyService`、`SignatureService`、`SettingService`、`PasswordService`
- **影响文件**：
  - `application/service/HttpService.php`
  - `application/service/NotifyService.php`
  - `application/service/SignatureService.php`
  - `application/service/SettingService.php`
  - `application/service/PasswordService.php`

### 控制器方法拆分

- **问题**：`createOrder()` 和 `appPush()` 方法过长，职责不清
- **修复**：`createOrder()` → `validateCreateOrderParams()` / `verifyCreateOrderSign()` / `adjustPrice()`；`appPush()` → `matchAndNotifyOrder()`
- **影响文件**：
  - `application/index/controller/Index.php`

## 🗄️ 数据库优化

### 存储引擎与字段类型

- **问题**：所有表使用 MyISAM 引擎，金额字段使用 `double` 类型，存在精度丢失
- **修复**：MyISAM → InnoDB；`double` → `decimal(10,2)`
- **影响文件**：
  - `vmq.sql`

### 索引缺失

- **问题**：高频查询缺少索引，全表扫描性能差
- **修复**：新增索引 `(state,type,really_price)`、`(state,create_date)`、`pay_id`、`order_id`(UNIQUE)、`tmp_price.oid`
- **影响文件**：
  - `vmq.sql`

## 🖥️ 前端优化

### 支付页面轮询策略

- **问题**：订单状态轮询固定间隔，服务器压力大
- **修复**：实现指数退避策略（1.5s→3s→5s→10s），添加 AJAX 错误回调处理
- **影响文件**：
  - `public/payPage/pay.html`

### 前端代码去重

- **问题**：`formatDate()` 在多个页面重复定义，jQuery 版本不统一
- **修复**：提取至公共模块 `common.js`，统一 jQuery 为 3.4.0
- **影响文件**：
  - `public/js/common.js`

## 🔄 兼容性说明

| 兼容项       | 说明                             |
| --------- | ------------------------------ |
| 旧版监控 App  | ✅ 完全兼容，MD5 签名正常工作              |
| 旧版商户对接    | ✅ 完全兼容，签名算法未变更                 |
| 旧版明文密码登录  | ✅ 兼容，自动升级为 bcrypt              |
| 数据库迁移     | ⚠️ 需执行 ALTER TABLE（详见 vmq.sql） |
| `.env` 配置 | ⚠️ 新部署需从 `.env.example` 复制     |

## 🎯 验证要点

1. ✅ 旧版监控 App 心跳上报正常（appHeart 接口）
2. ✅ 旧版监控 App 收款推送正常（appPush 接口）
3. ✅ 创建订单流程完整：签名验证 → 金额排重 → 订单创建 → 支付页面渲染
4. ✅ 管理员使用旧版明文密码登录成功，密码自动升级为 bcrypt 哈希
5. ✅ 后台仪表板数据正常显示（今日订单、今日金额等）
6. ✅ 系统设置保存成功，配置持久化
7. ✅ 订单管理列表正常，筛选、分页功能正常
8. ✅ 支付页面倒计时正常，轮询查单正常
9. ✅ 订单超时自动关闭（closeEndOrder）
10. ✅ 测试支付签名验证通过（example 目录示例）

***

## 📋 上游版本 \[v1.12] - 2020-01-30

| 项目   | 说明                                        |
| ---- | ----------------------------------------- |
| 项目名称 | V免签/vPay                                  |
| 仓库地址 | <https://github.com/szvone/vmqphp>        |
| 技术栈  | ThinkPHP 5.1 + MySQL + Layui + Vue.js 2.x |
| 核心功能 | 免签约支付接入（微信/支付宝）、安卓监控App、后台管理              |
| 状态   | **已停更**（最后提交 2020-01-30）                  |

**上游原始功能清单**：

- 支付订单模块：创建订单 / 查询订单 / 关闭订单 / 过期订单清理
- 监控端通讯：心跳上报 appHeart / 收款推送 appPush / 状态查询 getState
- 后台管理：仪表板 / 系统设置 / 订单管理 / 二维码管理 / 补单
- 支付页面：Vue.js 渲染、倒计时轮询、支付宝唤起
- 签名机制：MD5 签名验证
- 金额排重：tmp\_price + INSERT IGNORE + 微调递增/递减
- 二维码服务：endroid/qr-code 生成 + khanamiryan 识别

