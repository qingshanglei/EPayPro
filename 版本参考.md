

***

## 📋 版本 [1.0.9] - 2026-05-14

## 🔍 功能概述

本次更新修复了浏览器关闭后Cookie未回填数据库的问题，实现了Cookie数据的双向同步（数据库↔浏览器），确保用户在浏览器会话中产生的登录Cookie能够正确保存并在下次打开时自动注入。

## 🐛 问题修复

### 浏览器关闭后Cookie未回填数据库

- **问题**：
  - 用户新建浏览器环境并配置Cookie后，打开浏览器访问网站登录账号
  - 关闭浏览器后再次打开编辑界面时，Cookie配置字段显示为空
  - 再次打开同一环境时仍可自动登录，但编辑界面无法看到回填的Cookie数据
- **原因**：
  - 系统的Cookie数据流是单向的：仅在打开浏览器时从数据库读取Cookie注入浏览器（数据库→浏览器）
  - 关闭浏览器时不会从浏览器中读取Cookie回写到数据库（缺少浏览器→数据库的反向流程）
  - 用户在浏览会话中产生的Cookie仅保存在Chrome磁盘缓存中，不会同步回主数据库
  - 编辑界面读取数据库cookie字段时得到空值或旧值
- **修复**：
  - 新增 `_saveBrowserCookies` 方法：通过CDP协议在浏览器关闭前读取所有Cookie并保存到数据库（主动关闭场景）
  - 新增 `_saveCookiesFromDisk` 方法：从Chrome磁盘缓存读取Cookie并保存到数据库（浏览器自行关闭的降级方案）
  - 在 `batchClose` 和 `closeAll` 方法中集成Cookie回填逻辑，关闭前通过CDP读取Cookie，失败时降级为磁盘读取
  - 在 `handleBrowserClose` 闭包中集成Cookie回填逻辑，浏览器关闭后从磁盘读取Cookie
  - 数据库迁移添加 `cookie` 列兼容检查，确保旧版升级用户的数据完整性
  - `update` 方法增加Cookie保护逻辑，编辑环境其他字段时不覆盖已有的Cookie数据
- **影响文件**：
  - `src/main/server/service/WindowService.js`
  - `src/main/server/db/DbDao.js`
  - `src/main/server/db/WindowDB.js`

## 🎯 验证要点

1. ✅ 打开浏览器环境→访问网站登录→关闭浏览器→打开编辑界面，Cookie字段正确回显
2. ✅ 打开浏览器环境→访问网站登录→关闭浏览器→再次打开同一环境，仍可自动登录且Cookie已回填到数据库
3. ✅ 通过界面关闭按钮关闭浏览器，Cookie正确回填
4. ✅ 通过浏览器X按钮关闭浏览器，Cookie正确回填
5. ✅ 编辑环境其他字段（不修改Cookie），保存后Cookie数据不被覆盖为空